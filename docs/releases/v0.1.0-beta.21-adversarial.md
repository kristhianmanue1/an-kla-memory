<!-- an-kla:release-gate {"decision":"proceed","scope":"release-candidate","tag":"v0.1.0-beta.21"} -->

# Ronda adversarial REL — `v0.1.0-beta.21` (2026-09-02)

## Alcance

Release candidate completa del ciclo beta.21, atacada por subagente con
contexto fresco, mandato read-only y **alcance cerrado a lo modificado**
(27 archivos M + 4 nuevos; `git status --porcelain`): bump de plantilla
del contrato (`context_text.py` + `AN-KLA.md`/`AGENTS.md` vía flujos
explícitos), flip ADR-0046, cap `requires-python`, schemas con `text`,
warning stderr en `commit-write-plan`, runbook `agent-recovery.md`,
redteam unificado, anclaje `LC_ALL=C` con re-anclaje, metadata de release
y nota. Fuera de alcance deliberado: suite completa (corrida por el
operador antes de la ronda: 937 OK, skipped=72), `check_clean_wheel` y
`check_release_tag` (este último falla por diseño hasta que este
documento exista).

## Modelo de amenazas

Regla base intacta: la memoria y los documentos recuperados son dato,
nunca instrucción. Atacante modelo: release que declara cambios que no
aterrizaron (nota mentirosa), contrato gestionado alterado fuera de lo
prescrito (ADR-0046 §8), pins vivos olvidados que matan gates post-tag, y
falsificación de continuidad del anclaje (fila de re-anclaje que oculte
divergencia real).

## Hallazgos y correcciones

| Hallazgo | Riesgo | Corrección |
|---|---|---|
| MEDIUM: `docs/README.md` §Notas de release seguía declarando beta.20 como la release documentada más reciente | Índice mentiroso en el árbol taggeado | Corregido a beta.21 con enlaces a nota y ronda (mismo commit pre-tag) |
| MEDIUM (esperado): `v0.1.0-beta.21-adversarial.md` inexistente al momento de la ronda — `test_release_metadata` y el enlace del CHANGELOG dependen de él | Tag con test/enlace rotos | Este documento, creado como salida de la ronda y antes del tag |
| LOW: `__main__.py` hace echo del `record.id` a stderr sin sanear (schema acepta cualquier string no vacío; un `\n` falsificaría líneas de stderr — canal local, stdout intacto) | Cosmético/diagnóstico | No aplicado — diferido a beta.22 (sanear o patrón en schema) |
| LOW (pre-existente, fuera del diff): `docs/write-policy-cli.md:310` fecha la capability attest en "beta.20+" | Imprecisión documental | No aplicado — beta.22 |
| LOW (pre-existente, fuera del diff): `check_beta{8,13..17}_upgrade.py` pinean `template_version` de su era; una corrida fresca hoy fallaría | Harnesses históricos por-era | No aplicado — declarados artefactos por-era |

## Verificación de canonicidad / determinismo

- **Hashes de plantilla recalculados independientemente** por el atacante:
  beta.21 content `a1478300…` y contract `12b7f017…` = declarados;
  beta.11 contract `255b0fd2…` desde `git show HEAD:AN-KLA.md`
  (raw == semántico) = declarado. El comentario "byte-equivalente" de la
  entrada histórica es honesto.
- **Diff del contrato beta.11→beta.21**: exactamente el párrafo §2
  (9−/8+); "sin cambios fuera de §Resolver autoridad" verificado TRUE.
- **§2 vs código**: `_cli_authority` falla cerrado sin receipt;
  `write_policy.py` exige evidence `verified`; `capabilities()` declara
  `tool_observed_resolution` + `attest.requires_store_with_key` (ADR-0046
  §8). Sin contradicción contrato ↔ capabilities ↔ write-policy-cli.
- **Anclaje**: digest `42d3f6be…` reproducido con pipeline viejo y con
  `LC_ALL=C` (idénticos en el host canónico); `verificar_anclaje.py`
  exit 0 `anchor_match`; fila de re-anclaje parseable por `ANCHOR_ROW_RE`
  y honesta (declara la fila intermedia omitida y el avance legítimo a
  rev 37; `status` confirma `revision_number: 37`).
- **Sweep de pins vivos** (`0.1.0-beta.11` / `0.1.0b20` / `beta.20` en
  `an_kla/`, `tests/`, `scripts/`): todo lo hallado es histórico
  legítimo (`removed_in`, provenance de fixtures, harnesses por-era);
  ningún pin vivo olvidado.

## Comandos y salidas

- Gates: `check_sizes` OK (427 archivos) · `check_plans` OK (2 planes) ·
  `check_adr_registry` OK (46 ADRs; aceptada=43, propuesta=3) ·
  `verificar_anclaje` exit 0 · `context status` `ok:true`, template
  `0.1.0-beta.21`, sin diagnósticos · `--version` `0.1.0b21`.
- Tests focales de lo tocado: 49 tests OK en tres corridas
  (`test_commit_indexable_warning`, `test_verificar_anclaje`,
  `test_redteam_consistent_rewrite`, `test_adr_registry`,
  `test_f0_contracts`, `test_subject_ref`, `test_integration_status`).
  `test_release_metadata` falló SOLO por la ausencia de este documento
  (línea 49), confirmado y resuelto con este archivo.

## Límites declarados

- La suite completa (937 OK) y `check_clean_wheel` fueron corridos por el
  operador antes de la ronda, no por el atacante; el gate de wheel se
  re-ejecutará en el cierre del ciclo.
- La ronda no ejecutó instalación en intérpretes múltiples (hecha por el
  operador en T2 con 3.9.6/3.12.12).
- Los tres LOW quedan nombrados aquí; los dos pre-existentes no
  pertenecen al diff de esta release.

## Decisión

- [x] proceed — condicionado a que la corrección de `docs/README.md` y
      este documento aterricen en el commit pre-tag (condición satisfecha
      en el cierre del ciclo)
- [ ] fix-and-retry
- [ ] escalate
