# v0.1.0-beta.23 — Release de mantenimiento (2026-09-05)

Etiqueta: `v0.1.0-beta.23` · Versión: `0.1.0b23` · Base: `main` tras
`9f9da89`. **Release code-only**: contrato gestionado y `TEMPLATE_VERSION`
siguen en `0.1.0-beta.21` — sin flujo de contexto para actualizar.

## Contenido

1. **#119 (P1-beta.23) corregido**: `export create` volvía a funcionar con
   perfil host-managed — `hook-runs/**` viaja con la bóveda (`_PATTERNS`)
   y `host-hooks.json` queda excluido (decisión congelada de ADR-0048 §3).
   Test de regresión incluido.
2. **Pase de deuda pre-G3** (todo ya mergeado tras beta.22):
   - #115/T1: replay de ADR-0024 §API/CLI (mismo txid + mismo plan →
     `replayed: true`; binding distinto → `transaction_binding_conflict`).
   - #118/R2: lease único de reader gate en `assemble_context`.
   - #111/P2: `write_lock` POSIX con deadline de 10s (`write_lock_busy`).
   - #116/C1: `verificar_anclaje` fail-closed (quoting + pipefail + refs
     no vacío).
   - #117: `store.py` 812 → 573 líneas, partición en
     `write_commit.py`/`store_locks.py`/`store_recovery.py`/
     `store_errors.py`; techo transitorio retirado.
3. **ADR-0048 aceptada** (G3, store_root externo — Propuesta → Aceptada):
   registro en 48 ADRs (45 aceptadas, 3 propuestas). Sin implementación
   aún; F1 abre en la siguiente sesión.

## Verificación

- Suite canónica: **987 tests OK** tras el bump (corrida post-bump,
  lección de la ronda beta.22 aplicada).
- Gates: `check_sizes.py` OK (459 archivos), `check_plans.py` OK,
  `check_adr_registry.py` OK (48 ADRs: 45 aceptadas, 3 propuestas).
- Ronda REL: `v0.1.0-beta.23-adversarial.md`.

## Instalación

```bash
pip install "an-kla-memory @ git+https://github.com/kristhianmanue1/an-kla-memory.git@v0.1.0-beta.23"
```
