# Ronda adversarial REL — v0.1.0-beta.24 (2026-09-06)

Candidata: working tree posterior al bump (diff vs tag
`v0.1.0-beta.23` = 15 archivos: bump de versión, feature
`--emit-authority-template` + guía de checkpoint, ruta del agente,
aviso update-check, upgrade-agent-flow refrescado, acta/notas de esta
ronda). Método: auditor de contexto fresco, sólo lectura; venv limpio
py3.12 con `pip install .`; stores de ataque en tmp.

## Veredicto: `fix-and-retry` → absorbido → `proceed`

**Cero defectos de producto**: smoke completo del feature estrella
(template → plan write → commit → verify) en venv limpio, guía
reproducida en store con `git/v1`, upgrade/docs coherentes.

## Hallazgos y absorción

| ID | Sev | Hallazgo | Absorción |
|---|---|---|---|
| F1 | BLOQ | Notas de release y CHANGELOG §beta.24 inexistentes (objeto principal de la ronda ausente) | Escritos; el feature (559040e) declarado explícitamente como contenido de beta.24 (b23 no lo contiene: `git tag --contains` vacío) |
| F2 | HIGH | Acta ausente (gate + cascadas Row15) | Este documento |
| F3 | MED | Texto nuevo del aviso update-check sin aserción en `test_newer_version_produces_notice` | Aserciones añadidas (`context plan --operation update`, `uso-diario.md`) |
| F4 | MED | `docs/README.md` aún nombra b23 como última documentada | Refrescado a beta.24 |
| F5 | MED | Guía §3 no copy-paste-safe: `an_kla` binario puede no estar en PATH (exit 127), `uuidgen` da mayúsculas → `invalid_transaction_id`, `date +%f` BSD distinto → `invalid_working_state` (el validador atrapó todo, como documenta su §5) | §3 reescrita copy-paste-safe: `python -m an_kla`, TXID/STAMP/REVISION generados por python |

## Hallazgo no-cascada adicional (F-ronda b24, pre-existente)

`test_adapter_id_grammar_edges_accepted` (anidado, sealed) falló una vez
con `export_destination_exists`: el nombre de destino derivado de
`hash(good)` colisiona bajo ciertos `PYTHONHASHSEED` (~0.6%/proceso,
reproducido con seed 73). **Pre-existente** (sealed/ sin tocar por este
diff); derivado a issue de deuda con repro y seed.

## Checks con evidencia

- `git diff v0.1.0-beta.23..HEAD -- AGENTS.md AN-KLA.md` → vacío.
- Suite: 996 tests; fallos pre-absorción = release_metadata (notas/acta)
  + cascadas; post-absorción → **996 OK**.
- Smoke: venv py3.12 `pip install .` → `an-kla-memory 0.1.0b24`;
  init/status/verify; `--emit-authority-template` emite authority con
  `proposal_sha256`/`base_revision` reales; plan `write`; commit
  `committed: true`; `checkpoint show` checkpoint-v2.
- Guía reproducida: §3 corregida ejecutada en store2 con `git/v1`
  (HEAD caller_asserted) — verde.
- Aviso update-check: texto renderiza; aserciones nuevas en verde.
- `git tag --contains 559040e` → vacío (b23 no lo contiene): beta.24 lo
  incluye y las notas lo declaran.

## Decisión

Post-absorción (notas + changelog + acta + test del aviso + guía
copy-paste-safe) y suite en verde en el commit de release, la candidata
queda **`proceed`** para etiquetar `v0.1.0-beta.24` y publicar la
prerelease con el wheel como asset.
