# Ronda adversarial REL — v0.1.0-beta.22 (2026-09-05)

Candidata: commit `b197621` (wheel `an_kla_memory-0.1.0b22-py3-none-any.whl`,
construido desde ese árbol). Método: auditor de contexto fresco, sólo
lectura del repo; stores de ataque en tmp fuera del workspace; wheel
instalado en venv limpio para todos los smoke (nunca el checkout);
upgrade path con el wheel real de beta.21 descargado de GitHub Releases.

## Veredicto: `fix-and-retry` → absorbido → `proceed`

Cero defectos de producto (motor, schemas, hooks, upgrade path y gates
estructurales sanos; wheel byte-idéntico al árbol). Hallazgos de
**preparación de release**, absorbidos en el commit de fix:

| ID | Sev | Hallazgo | Absorción |
|---|---|---|---|
| F1 | HIGH | Suite en rojo en el candidato: 3 omisiones del bump — `test_release_metadata.py:82` exigía fragmento `@v0.1.0-beta.21`; `SECURITY.md` sin fila beta.22; `test_update_check.py:166` sin mapping `0.1.0b22`; `CITATION.cff` sin bump (descubierto en la re-corrida) (+ cascadas en meta-tests Row15) | Fragmento actualizado a beta.22; fila SECURITY añadida; mapping `0.1.0b22: 0.1.0-beta.22` añadido; CITATION.cff → `0.1.0-beta.22` / `2026-09-05`; suite re-corida en verde |
| F2 | MED | `docs/releases/v0.1.0-beta.22-adversarial.md` no existía (la nota lo lista y un gate lo exige) | Este documento |
| F3 | LOW | Claim numérico: README 255, notas decían 251 | Notas corregidas (251 tras partición, 255 tras bump) |
| F4 | INFO | "Partidos en 12": 12 archivos añadidos, 8 sucesores de partición | Redacción de la nota se mantiene; aclarado aquí |
| — | SKIP | Update-check del CLI antiguo descubre beta.22: imposible pre-publicación (la release no existe) | SKIP declarado; mecanismo verificado en rounds anteriores |

## Checks con evidencia (comando → resultado)

- `git diff v0.1.0-beta.21..b197621 -- AGENTS.md AN-KLA.md` → vacío
  (release code-only confirmada).
- Wheel en venv limpio: `--version` → `an-kla-memory 0.1.0b22`;
  `context status` fresco → `diagnostics: []` (TEMPLATE_VERSION sin
  cambio); schemas G2 embebidos.
- Suite completa del candidato → los 3 fallos F1 reproducidos en 3.9 y
  3.12 en aislado; tras absorción: **983 tests OK**.
- Plano no acuña / flag acuña (4 subcomandos); `action_mismatch` con
  exit 0; caps 64 KiB/2000; O_EXCL idempotente.
- #113/R1: `plan-write` con `status=[]` → `invalid_write_proposal
  (record.status)`; inyección por `MemoryStore.commit` → retrieve no
  crashea, excluye el veneno (`inactive`) y sirve el sano.
- v1 por defecto byte-idéntico (diff de salida v1 entre CLI b21 y b22:
  vacío); v2 computa perfil (`--now` +48h → `declared-not-invoked`,
  `pending_continuity: required`).
- Upgrade path real: wheel b21 (release GitHub) → store con fact →
  upgrade a b22 → `verify()` OK, fact recuperable.
- Coherencia: instalación fija `@v0.1.0-beta.22` (README/uso-diario);
  gates en verde; guía de hosts reproducida contra el wheel; 211
  `def test_` preservados; #56 CLOSED con acta; #117 OPEN (techo 820,
  archivo 812).

## Decisión

Tras absorber F1–F3 y re-verificar suite + gates en el delta del commit
de fix, la candidata queda **`proceed`** para etiquetar
`v0.1.0-beta.22` y publicar la prerelease con el wheel como asset.
