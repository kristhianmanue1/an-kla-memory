# Ronda adversarial REL — v0.1.0-beta.23 (2026-09-05)

Candidata: árbol HEAD posterior al bump (`9f9da89` + commit de release),
wheel `an_kla_memory-0.1.0b23-py3-none-any.whl` construido desde ese
árbol. Método: auditor de contexto fresco, sólo lectura; venv limpio para
los smoke; upgrade path con el wheel real de beta.22 descargado de
GitHub Releases.

## Veredicto: `fix-and-retry` → absorbido → `proceed`

**Cero defectos de producto.** Hallazgos exclusivamente de preparación
de release (espejo de la ronda beta.22):

| ID | Sev | Hallazgo | Absorción |
|---|---|---|---|
| F1 | HIGH | Wheel b23 aún no construido al momento de la ronda | Construido desde el commit de release; asset de la etiqueta |
| F2 | HIGH | Acta ausente (gate `test_release_metadata` + cascadas Row15) | Este documento |
| F3 | MED | Bump sin commit al momento de la ronda | Commit de release patrón `5bab8c4` (bump + acta) |
| F4 | INFO | `scripts/check_clean_wheel.py:57` hardcodeaba `0.1.0b21` (stale desde b21; no es gate) | Parametrizado con `an_kla.version.VERSION` |

## Checks con evidencia (comando → resultado)

- **code-only**: `git diff v0.1.0-beta.22..HEAD -- AGENTS.md AN-KLA.md`
  → vacío.
- **#119**: venv limpio + store con `hook-runs/` y `host-hooks.json` →
  `export create` OK; manifiesto incluye hook-runs y excluye
  host-hooks.json; `export verify` → `verified: true`. Control negativo
  con wheel b22 real (GitHub): reproduce
  `export_unrecognized_durable_path`.
- **T1 replay**: `commit_write_plan` con txid explícito → `committed:
  true`; replay idéntico → `committed: true, replayed: true`, misma
  revisión; plan distinto mismo txid → `transaction_binding_conflict`.
- **P2 lock**: deadline parcheado a 0.05s + `write_lock` anidado →
  `LockBusyError: write_lock_busy` (0.056s).
- **R2 lease**: `assemble_context` smoke OK (`context-assembly-v1`).
- **Suite**: 987 tests (conteo exacto); fallos pre-absorción = 1 raíz
  (acta ausente) + 4 cascadas Row15; post-absorción → OK.
- **Coherencia**: README/uso-diario instalan `@v0.1.0-beta.23`;
  SECURITY/CITATION/docs-README en beta.23; gates OK (48 ADRs: 45
  aceptadas, 3 propuestas); refs b22 residuales sólo históricas.
- **Upgrade path**: wheel b22 real → store con datos → upgrade b23 →
  `--version` `0.1.0b23`, `verify` ok, revisión preservada, `retrieve`
  sirve el fact pre-upgrade.
- **Partición #117**: desde el wheel importan `MemoryStore`,
  `LockBusyError`, `IntegrityError`, `StoreError`,
  `ConcurrentUpdateError`, `STREAMS`, `commit_write_plan`, `write_lock`;
  `store.py` 573 líneas (vs 812 en b22).

## Decisión

Tras absorber F1–F3 y re-correr suite + gates en el commit de release,
la candidata queda **`proceed`** para etiquetar `v0.1.0-beta.23` y
publicar la prerelease con el wheel como asset.
