# Ronda adversarial — `v0.1.0-beta.19`

> Ronda adversarial de release (REL) ejecutada el 2026-08-28 por un panel
> con contexto fresco y mandato refutador, sobre la rama
> `run/beta19-preparacion` (commit `4614e8b`, base `4486270f`). Cierra la
> condición C-1 de la [REL beta.18](v0.1.0-beta.18-adversarial.md): esta
> auditoría precede a la creación de la etiqueta `v0.1.0-beta.19`.

## Alcance (candidata)

Release documental: deuda C-2 de la REL beta.18 (H-5 CHANGELOG/índice/
wheels + H-6 `check_clean_wheel`), partición de ADR-0042 en ADR corto +
apéndice técnico (#95) y bump `0.1.0b18` → `0.1.0b19`. Sin cambios de
código de runtime: `an_kla/` no muta salvo `version.py`.

## Gate de publicación

| Gate | Comando | Resultado |
|---|---|---|
| Suite canónica | `.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` | **OK — 886 tests, skipped=72, exit 0** (292 s) sobre el árbol candidato |
| Tamaños | `scripts/check_sizes.py` | `check_sizes: OK`, exit 0, sin consumo de gracia (ADR-0042: 198 líneas vs límite duro 400) |
| Wheel limpio | `scripts/check_clean_wheel.py` | `check_clean_wheel: OK … (an-kla-memory 0.1.0b19)`, exit 0 |
| Registro ADR | `scripts/check_adr_registry.py` | `OK — 42 ADRs (aceptada=40, propuesta=2)`, exit 0 |
| Gate de tag (C-1) | `scripts/check_release_tag.py v0.1.0-beta.19` | `release_gate_missing` (exit 1) ANTES de esta ronda — esperado: el marcador lo introduce esta decisión; tras escribirla, exit 0 (verificado abajo) |

## Hallazgos y correcciones

| ID | Severidad | Hallazgo | Disposición |
|---|---|---|---|
| H-1 | VERIFICADO (integridad) | `git log --oneline main..run/beta19-preparacion` → único commit `4614e8b`; `git diff main..run/beta19-preparacion --stat` → exactamente los 14 archivos declarados; sin cambios en `.github/`. Base confirmada `4486270f` (`git merge-base`). | Cumple lo declarado. |
| H-2 | VERIFICADO (coherencia) | `an_kla/version.py` = `0.1.0b19`; README (badge, texto, comandos pip, `upgrade --target`), SECURITY (tabla ✅/❌), CITATION.cff (`version: "0.1.0-beta.19"`), CHANGELOG (entrada propia, «No publicadas» vacío) y `docs/README.md` (release documentada más reciente = beta.19; última publicada con REL cerrada = beta.18) sincronizados. Residuales `b18` sólo en historia (mapping del update-check, REL beta.18, changelog histórico, gate de upgrade beta.17→18 por diseño). | Sin contradicciones. |
| H-3 | VERIFICADO (desviación) | `git diff` de `tests/test_release_metadata.py` = exactamente 1 línea: pin `v0.1.0-beta.18` → `v0.1.0-beta.19` (precedente `1d1e1cb`). Sin nada escondido. Adicional no-declarado-como-desviación pero declarado en la nota: `tests/test_update_check.py` +1 línea en el mapping (`0.1.0b19` → `0.1.0-beta.19`), metadato de fixture, mismo tipo que el fix H-3b de beta.18. | Desviación aceptada se valida 1:1. |
| H-4 | VERIFICADO (sin cambios funcionales) | Diff de código de producto = `an_kla/version.py` (2 líneas: bump). El resto son `scripts/check_clean_wheel.py` (pin b19), los 2 fixtures de test y documentación. Ningún módulo de runtime muta. | Cumple el alcance documental declarado. |
| H-5 | BAJA (documental) | La nota de release afirma que `check_sizes.py` pasa «sin la gracia de TECH_DEBT, **que se retira**»: la gracia no se consume (el ADR cabe en el límite duro), pero la entrada `TECH_DEBT["docs/architecture/0042-sealed-export-v1.md"]` —con comentario obsoleto «622 líneas pendiente de partir»— sigue presente en `scripts/check_sizes.py` (el archivo no fue tocado por la rama). Entrada muerta, no bloqueante. | Condición correctiva C-A: retirar la entrada y su comentario en el próximo ciclo documental. |
| H-6 | BAJA (documental) | El apéndice técnico declara contener el «desglose normativo … de §1–§9», pero sus encabezados son §1, §2 (schemas), §2 (gramática CLI, encabezado duplicado), §3, §4, §6, §7, §8, §9 — **no existe encabezado §5**. El contenido normativo de §5 (enum fail-closed) vive en la bala §5 de la sección Decisión del ADR corto, y los códigos del enum aparecen dispersos en el apéndice (§6/§8/§9); la tabla fila-a-fila del viejo §5 quedó condensada. Las 14 anclas `ADR-0042 §N` citadas por `an_kla/`, `tests/`, `docs/` y schemas resuelven sin ambigüedad (§5 apunta al ADR, nunca al apéndice — verificado por grep). | Condición correctiva C-B: renombrar el segundo «§2» (p. ej. §2b), añadir nota explícita de que §5 se regula en el ADR corto, o incorporar encabezado §5 al apéndice. |
| H-7 | INFO (pre-existente) | `CITATION.cff` conserva `date-released: "2026-08-12"` (heredado de main; no es regresión de esta rama; el tag beta.19 aún no existe). | Sin acción en este ciclo; actualizar al publicar. |
| H-8 | VERIFICADO (proceso C-1) | `git tag --list` → no existe `v0.1.0-beta.19`; `gh release list` → última release `v0.1.0-beta.18` (2026-08-28). Secuencia REL→tag intacta. | Cumple C-1. |
| H-9 | VERIFICADO (nota de release) | `v0.1.0-beta.19.md` declara Windows como limitación conocida diferida por el operador (procedible macOS/Linux), cita la REL beta.18 y describe exactamente el alcance real (documental + bump). Windows NO se re-abre. | Cumple. |

## Verificación de canonicidad / determinismo

- Árbol auditado = HEAD de `run/beta19-preparacion` = `4614e8b`, working
  tree limpio al inicio y al cierre de la ronda (única escritura: este
  documento, autorizada por el mandato de la ronda).
- Partición ADR-0042 coherente entre ADR corto (198 líneas:
  Contexto/Decisión/Por qué no/Consecuencias/Límites/Test de regresión/
  Referencias) y apéndice (483 líneas); el ADR remite al apéndice en tres
  puntos y `docs/README.md`/`docs/sealed-export-guide.md` actualizan sus
  rutas sin contradicción (ver H-6 para la salvedad §5).
- `check_clean_wheel.py` instaló el wheel aislado y verificó
  `an-kla-memory 0.1.0b19` — el gate H-6 de beta.18 vuelve a estar
  ejercitable contra la candidata.
- La suite canónica corre verde incluyendo
  `test_release_documents_match_the_runtime_candidate`,
  `test_readme_covers_reproducible_consumer_lifecycle` y el mapping de
  update-check, que era el modo de fallo POSIX de beta.18 (H-3).

## Condiciones del veredicto

- **C-A (documental, correctiva)**: en el próximo ciclo, retirar la
  entrada muerta de `TECH_DEBT` (y su comentario obsoleto) de
  `scripts/check_sizes.py`, dejando el gate sin gracia real.
- **C-B (documental, correctiva)**: en el próximo ciclo, sanear la
  numeración del apéndice técnico (encabezado §5 o nota de delegación al
  ADR corto; resolver el «§2» duplicado).
- **C-1 (heredada de beta.18, cumplida en el orden correcto)**: esta
  decisión `proceed` precede al tag; `check_release_tag.py
  v0.1.0-beta.19` debe pasar sobre el árbol que incluye este documento
  antes de cualquier push de etiqueta.
- **Limitación heredada (sin re-apertura)**: Windows permanece diferido
  por decisión del operador; esta release se declara procedible para
  macOS/Linux.

## Decisión

- [x] proceed
- [ ] fix-and-retry
- [ ] escalate

`proceed` registrado el 2026-08-28 por el panel REL (subagente de ronda
adversarial de release, contexto fresco, mandato refutador) para la
candidata `0.1.0b19` sobre el árbol de `run/beta19-preparacion`
(`4614e8b`) más este documento. Suite 886 OK / 0 fallas / 72 skips
 POSIX; sin cambios funcionales no declarados; desviación aceptada
 validada 1:1; hallazgos H-5/H-6 son documentales y quedan como
 condiciones correctivas C-A/C-B, no bloqueantes. Autoriza crear la
 etiqueta `v0.1.0-beta.19` sobre el árbol que contiene esta decisión y
 el marcador inferior.

<!-- an-kla:release-gate {"decision":"proceed","scope":"release-candidate","tag":"v0.1.0-beta.19"} -->
