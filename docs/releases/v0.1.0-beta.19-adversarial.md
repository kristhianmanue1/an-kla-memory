# Ronda adversarial — `v0.1.0-beta.19`

> **ESTADO: PENDIENTE.** Este documento es el lugar canónico donde la
> ronda adversarial de release (REL) de la candidata `0.1.0b19` debe
> registrar su auditoría y su única decisión `proceed` válida
> (`docs/practicas-ingenieria.md` §1/§9). La etiqueta `v0.1.0-beta.19`
> **no debe crearse** mientras este documento no contenga esa decisión
> (condición C-1 de la REL beta.18). Se crea vacío-por-diseño en la
> preparación de release para que la nota y los índices no citen una
> ruta inexistente.

## Alcance (candidata)

Release documental: deuda C-2 de la REL beta.18 (H-5 CHANGELOG/índice/
wheels + H-6 `check_clean_wheel`), partición de ADR-0042 en ADR corto +
apéndice técnico (#95) y bump `0.1.0b18` → `0.1.0b19`. Sin cambios de
código de runtime: `an_kla/` no muta salvo `version.py`.

## Gate de publicación

- Suite canónica (`.venv/bin/python -m unittest discover -s tests -p
  'test_*.py'`) → exit 0 sobre el árbol candidato.
- `scripts/check_sizes.py` → exit 0 SIN gracia de ADR-0042.
- `scripts/check_clean_wheel.py` → exit 0 con `0.1.0b19`.
- Coherencia CHANGELOG/docs/README/versión sin contradicciones.

## Hallazgos y correcciones

(pendiente — la ronda REL los registra aquí)

## Verificación de canonicidad / determinismo

(pendiente — la ronda REL los registra aquí)

## Decisión

- [ ] proceed
- [ ] fix-and-retry
- [ ] escalate

(pendiente — la etiqueta exige `proceed` válido sobre el árbol exacto
que este documento describa)
